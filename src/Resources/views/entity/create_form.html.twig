
{{ form_start(form) }}

{{ form_row(form.namespace) }}
{{ form_row(form.bundlePath) }}
{{ form_row(form.name) }}
{{ form_row(form.isDatabase) }}


<table class="table table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>or select</th>
            <th>Type</th>
            <th>Is form</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    {% for field in form.entityField %}
        <tr>
            <td>{{ form_row(field.name) }}</td>
            <td>{{ form_row(field.templateName) }}</td>
            <td>{{ form_row(field.type) }}</td>
            <td>{{ form_row(field.isForm) }}</td>
            <td>
            </td>
        </tr>

    {% endfor %}
    <tr>
        <td colspan="4"></td>
        <td><button
                    type="submit"
                    class="btn btn-info"
                    onclick="document.getElementById('entity_info_form_sysAction').value ='add-type';">
                +
            </button></td>
    </tr>
    </tbody>
</table>

<a href="{{ path('admin_maker_entity_index') }}" class="btn btn-dark">Cancel</a>
<button type="submit"
        class="btn btn-primary"
        onclick="document.getElementById('entity_info_form_sysAction').value ='save';"
>Save</button>

{{ form_end(form) }}